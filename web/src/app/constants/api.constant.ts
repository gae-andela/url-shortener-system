/**
 * For avoiding typo when needing endpoint URLs
 */
export const API_ENDPOINTS = {
  auth: {
    root: 'auth',
    who: 'who',
    signUp: 'sign-up',
    signIn: 'sign-in',
  },

  urls: {
    root: 'urls',
    id: ':id',
    stats: ':id/stats',
    redirect: 'xyz',
  },
};

export const URL_PARAMETERS = {
  id: ':id',
};
